# Модуль: module_1_3.py
# Цель: Демонстрация динамической типизации в Python

# Создание переменной 'name' типа string и присвоение ей значения
name = "Сергей"
# Вывод значения переменной 'name'
print(f"Name: {name}")

# Создание переменной 'age' типа int и присвоение ей значения
age = 25
# Вывод значения переменной 'age'
print(f"Age: {age}")

# Обновление значения переменной 'age' путем прибавления нового значения
age += 5  # Добавляем 5 к текущему возрасту
# Вывод обновленного значения переменной 'age'
print(f"New Age: {age}")

# Создание переменной 'is_student' типа bool и присвоение значения
is_student = True
# Вывод значения переменной 'is_student'
print(f"Is Student: {is_student}")

# Этот код демонстрирует динамическую типизацию:
# переменные в Python могут изменять свои значения, и их тип определяется автоматически при присвоении.
# Теперь запускай код, и программа всё покажет! 😊

# В модуле 'module_1_3.py' 2 вариант

# 1. Создаём переменную 'name' и присваиваем ей значение вашего имени (строка).
name = "Сергей"  # Здесь можно указать своё имя
# Выводим значение переменной 'name' на экран.
print("Name:", name)

# 2. Создаём переменную 'age' и присваиваем ей значение вашего возраста (целое число).
age = 54  # Здесь можно указать ваш возраст
# Выводим значение переменной 'age' на экран.
print("Age:", age)

# 3. Перезаписываем значение переменной 'age', добавляя к текущему значению новое число.
# Например, добавим 1 год к возрасту.
age = age + 1  # К текущему значению 'age' добавляем 1
# Выводим изменённое значение переменной 'age' на экран.
print("New Age:", age)

# 4. Создаём переменную 'is_student' и присваиваем ей значение True (логическое значение).
is_student = True  # Это означает, что я являюсь студентом
# Выводим значение переменной 'is_student' на экран.
print("Is Student:", is_student)
